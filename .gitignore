# Generated reports ARE committed (the daily task commits and pushes them to git).
# Only logs and local environment artifacts are ignored.

# Logs (not committed)
logs/
__pycache__/
*.pyc

# Local environment & cache (not committed)
.venv/
wheels/
